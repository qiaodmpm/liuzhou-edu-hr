# Frontend Project Template

This is the minimal starter template used by the automation initializer.

## Included

- static homepage
- `npm run build`
- Docker build support
- Kubernetes manifests
- lightweight docs under `docs/`
- Ingress host pattern based on `${SERVICE_NAME}.cicd.chaodao.cn`

## Build

```bash
npm install
npm run build
```
